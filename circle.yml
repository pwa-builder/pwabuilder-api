machine:
    node:
        version: 0.12.2
dependencies:
    override:
        - npm install
test:
    override:
      - echo "No tests"
deployment:
    staging:
        branch: 1.0.0
        commands:
          - ./build/push.sh
